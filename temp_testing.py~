from database import *
users = DataBase("users", "first", "last")
users.add_entry("Connor", "Dewar")
users.query("select * where first is Connor")
users.add_entry("Yolo", "Dewar")
users.query("select * where last is Dewar")
